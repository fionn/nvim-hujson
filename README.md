# HuJSON Filetype Plugin

This will:
* define a new `hujson` filetype, mostly inherited from `jsonc`,
* set `formatprg` to [`hujsonfmt`](https://github.com/tailscale/hujson?tab=readme-ov-file#formatting-with-hujsonfmt),
* indent with tabs,
* format on save, if `hujsonfmt` is installed.
